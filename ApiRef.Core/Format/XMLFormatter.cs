using System;
using System.Xml;

namespace ApiRef.Core.Format
{
    public static class XMLFormatter
    {
        public static void Format(this XmlNode member, MarkdownBuilder builder, int titleSize)
        {
            for (int i = 0; i < member.ChildNodes.Count; i++)
            {

            }
        }
    }
}